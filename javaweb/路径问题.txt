1、文件流的读取：
相对路径：
绝对路径：

2、配置文件：
类加载器读取：从bin文件夹下开始读取

3、超链接，表单提交
从web.xml中读取servlet的名称

4、请求转发路径
从web.xml中读取servlet的映射路径
请求的转发: /代表的是当前 WEB 应用的根目录：localhost:8080/day30/
请求的重定向: / 代表的是当前 WEB 站点的根目录：localhost:8080/

5. 使用绝对路径：使用相对路径可能会有问题, 但使用绝对路径肯定没有问题. 

1). 绝对路径： 相对于当前 WEB 应用的路径. 在当前 WEB 应用的所有的路径前都添加 contextPath 即可. 

2). / 什么时候代表站点的根目录, 什么时候代表当前 WEB 应用的根目录

若 / 需要服务器进行内部解析, 则代表的就是 WEB 应用的根目录. 若是交给浏览器了, 则 / 代表的就是站点的根目录
若 / 代表的是 WEB 应用的根目录, 就不需要加上 contextPath 了. 